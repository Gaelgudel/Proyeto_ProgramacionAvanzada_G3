@page
@model ProyectoG3.Web.Pages.Cajas.VerSinpeModel
@{
    ViewData["Title"] = "Todos los SINPE";
}

<h2>Lista de todos los SINPE registrados</h2>

<a asp-page="/Cajas/Index"
   asp-route-idComercio="@Model.Caja.IdComercio"
   class="btn btn-secondary mb-3">
    ← Volver a Cajas
</a>

@if (!Model.Sinpes.Any())
{
    <div class="alert alert-info">No hay pagos SINPE registrados.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-bordered table-striped">
            <thead class="table">
                <tr>
                    <th>Teléfono Origen</th>
                    <th>Nombre Origen</th>
                    <th>Teléfono Destino</th>
                    <th>Nombre Destino</th>
                    <th>Monto</th>
                    <th>Descripción</th>
                    <th>Fecha</th>
                    <th>Estado</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Sinpes)
                {
                    <tr>
                        <td>@item.TelefonoOrigen</td>
                        <td>@item.NombreOrigen</td>
                        <td>@item.TelefonoDestinatario</td>
                        <td>@item.NombreDestinatario</td>
                        <td>@item.Monto.ToString("C")</td>
                        <td>@(item.Descripcion ?? "—")</td>
                        <td>@item.FechaDeRegistro.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>
                            <span class="badge @(item.Estado ? "bg-success" : "bg-secondary")">
                                @(item.Estado ? "Sincronizado" : "No sincronizado")
                            </span>
                        </td>
                        <td>
                            <a asp-page="/Cajas/AsignarSinpe"
                               asp-route-idSinpe="@item.IdSinpe"
                               class="btn btn-primary btn-sm">
                                Ligar a Caja
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}